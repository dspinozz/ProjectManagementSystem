FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
WORKDIR /app
EXPOSE 8080

FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

# Copy solution and project files for restore
COPY ProjectManagementSystem.sln .
COPY ["src/ProjectManagementSystem.UI/ProjectManagementSystem.UI.csproj", "src/ProjectManagementSystem.UI/"]
COPY ["src/ProjectManagementSystem.Domain/ProjectManagementSystem.Domain.csproj", "src/ProjectManagementSystem.Domain/"]

# Restore dependencies
RUN dotnet restore "src/ProjectManagementSystem.UI/ProjectManagementSystem.UI.csproj"

# Copy all source files
COPY src/ProjectManagementSystem.UI/ src/ProjectManagementSystem.UI/
COPY src/ProjectManagementSystem.Domain/ src/ProjectManagementSystem.Domain/

# Build the application
WORKDIR "/src/src/ProjectManagementSystem.UI"
RUN dotnet build "ProjectManagementSystem.UI.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "ProjectManagementSystem.UI.csproj" -c Release -o /app/publish /p:UseAppHost=false /p:PublishReadyToRun=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "ProjectManagementSystem.UI.dll"]
